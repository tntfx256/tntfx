{
  "$schema": "https://json.schemastore.org/tsconfig",

  "display": "react",
  "extends": "./base.json",
  "compilerOptions": {
    "jsx": "preserve",
    "lib": ["DOM", "DOM.Iterable", "ESNext"]
  }
}
